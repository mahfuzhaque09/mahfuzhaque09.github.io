# Project Rules & Guidelines

- **Testing**: Do not use Playwright or browser testing tools (such as `browser_subagent` or Playwright MCP) to test this website. Validate changes through direct code inspection, syntax checking, and local script verification instead.
